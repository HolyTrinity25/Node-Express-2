const timeWord = require('./timeWord');

describe('#timeword', () => {
  test('it is a function', () => {
    expect(typeof timeWord).toBe('function');
  });
  test('00:00', () => {
    expect(typeof timeWord).tobe('function');
  });
  test('00:12', () => {
    expect(typeof timeWord).tobe('function');
  });
  test('01:00', () => {
    expect(typeof timeWord).tobe('function');
  });
  test('6:01', () => {
    expect(typeof timeWord).tobe('function');
  });
  test('6:10', () => {
    expect(typeof timeWord).tobe('function');
  });
  test('6:18', () => {
    expect(typeof timeWord).tobe('function');
  });
  test('6:30', () => {
    expect(typeof timeWord).tobe('function');
  });
  test('10:34', () => {
    expect(typeof timeWord).tobe('function');
  });
  test('12:09', () => {
    expect(typeof timeWord).tobe('function');
  });
  test('12:09', () => {
    expect(typeof timeWord).tobe('function');
  });
  test('23:23', () => {
    expect(typeof timeWord).tobe('function');
  });

});
